name: main
on:
  push:
jobs:
    build:
        runs-on: ubuntu-latest
        steps: 
            - name: step1
              run: echo "step1"
            - name: step1
              run: echo "step2"
